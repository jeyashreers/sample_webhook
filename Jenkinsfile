pipeline {

    // run on jenkins nodes tha has slave label .....

    agent any

    
    stages {
        
        stage('Build') {
            steps {
                // Run the maven build
                echo "this is build step"
                
            }
        }
    }
}
